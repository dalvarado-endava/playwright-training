{
  "name": "javascript",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "npx playwright test --headed",
    "test:chromium": "npx playwright test --project=chromium",
    "test:chromium-headed": "npx playwright test --project=chromium --headed",
    "test:report": "allure generate ./allure-results -o ./allure-report --clean && allure open ./allure-report",
    "test:report-clean": "rm -r allure-results/ allure-report/ test-results/"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.25.2",
    "allure-playwright": "^2.0.0-beta.19"
  },
  "dependencies": {
    "dotenv": "^16.0.2"
  }
}
